From 9b80ec5c4afc0562eae7698b3c495b208b34cfdb Mon Sep 17 00:00:00 2001
From: Jeff Booher-Kaeding <jeff.booher-kaeding@arm.com>
Date: Thu, 8 Jul 2021 01:51:05 -0500
Subject: [PATCH 1/1] [PATCH] Fix for dmicheck test crashes with Sig7
 Signed-off-by: G Edhaya Chandran <edhaya.chandran@arm.com>

---
 src/dmi/dmicheck/dmicheck.c | 20 ++++++++++++++++----
 1 file changed, 16 insertions(+), 4 deletions(-)

diff --git a/src/dmi/dmicheck/dmicheck.c b/src/dmi/dmicheck/dmicheck.c
index 7f6a90c4..33290c41 100644
--- a/src/dmi/dmicheck/dmicheck.c
+++ b/src/dmi/dmicheck/dmicheck.c
@@ -390,8 +390,14 @@ static void* dmi_table_smbios(fwts_framework *fw, fwts_smbios_entry *entry)
 			return NULL;
 		}
 		table = malloc(length);
-		if (table)
-			memcpy(table, mem, length);
+		if (table){
+			if (fwts_safe_memcpy(table, mem, length) != FWTS_OK) {
+				fwts_log_info(fw, "Could not copy to SMBIOS table from %p for length %lu", (void *)mem, length);
+				(void)fwts_munmap(mem, length);
+				free(table);
+				return(NULL);	
+			}
+		}
 		(void)fwts_munmap(mem, length);
 		return table;
 	}
@@ -438,8 +444,14 @@ static void* dmi_table_smbios30(fwts_framework *fw, fwts_smbios30_entry *entry)
 			return NULL;
 		}
 		table = malloc(length);
-		if (table)
-			memcpy(table, mem, length);
+		if (table) {
+				if (fwts_safe_memcpy(table, mem, length) != FWTS_OK) {
+				fwts_log_info(fw, "Could not copy to SMBIOS30 table from %p for length %lu", (void *)mem, length);
+				(void)fwts_munmap(mem, length);
+				free(table);
+				return(NULL);	
+			}
+		}
 		(void)fwts_munmap(mem, length);
 		return table;
 	}
-- 
2.25.1

